# Do not use the Debian example NAS clients. The only static client is local
# testing inside the FreeRADIUS container; real router NAS entries are read
# from the isolated SQL `radius.nas` table at FreeRADIUS startup.
client localhost {
        ipaddr = 127.0.0.1
        secret = $ENV{RADIUS_LOCAL_TEST_SECRET}
}
